/** Camera look mode preferred when a match starts. */
export type CameraDefault = "locked" | "free";

/** GPU/CPU visual budget for the Three.js client. */
export type GraphicsQuality = "low" | "medium" | "high";
