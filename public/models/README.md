# Models

**Not used in-game by default.**

Gameplay uses the **procedural tactical soldier** (RUSH-B look: low-poly gear, team colors, readable gun).

`soldier.glb` may exist as an unused sample from earlier experiments. Do **not** auto-load it until it is a real military low-poly asset (idle/walk clips). Placeholders like CesiumMan break the visual target.
